<!DOCTYPE html>
<html>
<head>
  <title>Lessons | GLA English App</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <h1>Available Lessons</h1>
  
  <div class="lessons">
    <% lessons.forEach(lesson => { %>
      <div class="lesson-card">
        <h2><%= lesson.title %></h2>
        <div><%- lesson.content %></div>
      </div>
    <% }) %>
  </div>
</body>
</html>
