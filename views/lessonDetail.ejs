<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1.0">
<title><%= title %></title>
<script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen p-6">

<a href="/dashboard" class="text-blue-600 hover:underline mb-4 inline-block">← Back to Lessons</a>

<h1 class="text-3xl font-bold mb-6"><%= lesson.title %></h1>

<% lesson.modules.forEach(module => { %>
  <div class="bg-white shadow rounded p-4 mb-4">
    <h2 class="text-xl font-semibold mb-2"><%= module.title %></h2>
    <ul class="list-disc list-inside text-gray-700">
      <% module.topics.forEach(topic => { %>
        <li>
          <a href="/dashboard/lessons/<%= lesson.slug %>/<%= encodeURIComponent(topic.name) %>" 
             class="text-blue-600 hover:underline">
             <%= topic.name %>
          </a>
        </li>
      <% }) %>
    </ul>
  </div>
<% }) %>

</body>
</html>
